renderProgramdan çıkartılan dll içerisinde assimpin dll'ini göremiyor --> (bu sorun daha öncesinde opengl.dll'i system32 içerisine atılarak çözüldü güzel bir çözüm değil)
	*cmake ile assimp.dll'in ana dllye bağlanması gerekiyor (aynı şekilde opengl.dll'de)
	*ilk dll çıkartırken ki hatalar da glwf3.dll'den dolayı olabilir mi ? nasıl çözüldü ?


OSMAN


mesh setup her setup için üzerine mi biriktiyor yoksa ilk silip sonra mı ekliyor ? (mesh.clear fonksiyonu mu çağırılmalı) ? 


c# kısmındaki model kavramı çok saçma ?
	*model kavramı sadece fazla meshleri tutmak için var
	*cpp kısmında ise zaten modelloading görevi için duruyor çok gereksiz?
	*asıl modellerin render işlemini meshler yapabilir model classı ise modelloader olarak kalır ?
	*pozisyon ve sizelar modelden ayrılacak ?


meshte diffuse ve specular ne arıyor ?
lookat dll fonksiyonu kötü yazılmış

gl math fonksiyonları (rotate, scale, transform) entegre edilecek
	*işlemler c#ta olur c++ya model matrixsi dönebilir

model loading olunca c#taki sistem hiçbir şeyden etkilenmiyor

*DLL test projesi içerisindeki scriptleri kategorileştir namespaceleri ayarla

*ECS
	*entegresyon için kapatılan debug.assertleri daha sonrasında aç